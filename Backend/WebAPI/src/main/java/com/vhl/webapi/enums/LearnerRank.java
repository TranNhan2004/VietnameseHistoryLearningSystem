package com.vhl.webapi.enums;

public enum LearnerRank {
    BEGINNER,               // 0 <= point <= 1500
    INTERMEDIATE,           // 1501 <= point <= 5000
    ADVANCED,               // 5000 <= point <= 10000
    EXPERT                  // point > 10000
}
